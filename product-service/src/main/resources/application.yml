management:
  endpoints:
    web:
      exposure:
        include: '*'
server:
  port: 8083
spring:
  application:
    name: product-service
  datasource:
    password: sa
    url: jdbc:h2:mem:testDB
    username: sa
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
logging:
  file:
    name: product-ws.log
